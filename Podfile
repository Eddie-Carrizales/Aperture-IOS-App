# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Aperture' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Aperture

  # Add Parse pods and install
  pod 'Parse'
  pod 'AlamofireImage'
  pod 'MessageInputBar'

  target 'ApertureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ApertureUITests' do
    # Pods for testing
  end

end
